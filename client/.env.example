# Client env vars must be prefixed with VITE_ to be exposed to the app.
# The dev server proxies /api to the backend (see vite.config.ts), so no
# API base URL is needed for local development.

# Example:
# VITE_SOME_FEATURE_URL="https://example.com"
